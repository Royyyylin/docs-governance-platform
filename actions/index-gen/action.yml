name: 'Docs Index Generator'
description: 'Regenerate docs/index.md from frontmatter'

runs:
  using: 'composite'
  steps:
    - name: Generate index
      shell: bash
      run: bash "${{ github.action_path }}/gen-index.sh"
